"""RevMem CLI — the live agent-working view (Person C).

CLI-forward: the demo's hero is the agent's behavior streamed as a terminal
transcript, not a web dashboard. Reputation and routing are inline overlays.
"""
