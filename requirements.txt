# RevMem dependencies (shared). Person C needs: rich, resend, fastapi, uvicorn,
# pydantic. Person A/B: add google-genai, pymongo[srv], etc. as those land.
rich>=13.7
resend>=2.0
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
python-dotenv>=1.0
