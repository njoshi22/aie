[pytest]
pythonpath = .
# Narrow filter: upstream starlette/httpx2 deprecation — not our code, migration out of scope.
filterwarnings =
    ignore:Using `httpx` with `starlette\.testclient` is deprecated:starlette.exceptions.StarletteDeprecationWarning
